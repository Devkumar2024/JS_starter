// pattern printing questions
// left + right, if both are string then it converts other oerand as string and joins them
let row = 10, col=10;
for(let i=1; i<=row; i++){
    //main code
    let str = "";
    for(let j=1; j<=col; j++){
        str += "*";
        // * printed m times
    }
    console.log(str);
}